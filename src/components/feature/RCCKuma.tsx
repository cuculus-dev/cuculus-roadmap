'use client';

import { css } from '@kuma-ui/core';

export const RCCKuma = () => (
  <div
    className={css`
      font-size: x-large;
      font-weight: bold;
    `}
  >
    use client 🐻!
  </div>
);

export default RCCKuma;
