import { css } from '@kuma-ui/core';
export const RSCKuma = () => (
  <div
    className={css`
      font-size: large;
      font-weight: bold;
    `}
  >
    use server 🐻!
  </div>
);

export default RSCKuma;
