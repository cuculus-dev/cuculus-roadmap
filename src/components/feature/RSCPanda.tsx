import { css } from '@/styled-system/css';

const RSCPanda = () => {
  return (
    <div
      className={css`
        font-size: large;
        font-weight: bold;
      `}
    >
      use server 🐼!
    </div>
  );
};

export default RSCPanda;
